<tr>
  <td class="label"><label>Laboratoire : </label></td>
  <td><%= f.select :laboratory_id_equals, Laboratory.all.collect {|s| [ s.name, "#{s.id}" ] }, {:include_blank => true, :selected => params[:search] ? "#{params[:search][:laboratory_id_equals]}" : nil}, :class => "select_refine_search" %></td>
</tr>
<% if current_user %>
    <tr>
      <td class="label"><label>Entité de recherche : </label></td>
      <td><%= f.select :correspondents_search_entity_id_equals, SearchEntity.all.collect {|c| [ c.name, "#{c.id}"] }, {:include_blank => true, :selected => params[:search] ? "#{params[:search][:correspondents_teams_id_equals]}" : nil}, :class => "select_refine_search" %></td>
    </tr>
    <tr>
    <td class="label"><label>Correspondant : </label></td>
    <td><%= f.select :correspondents_id_equals, Correspondent.all.collect {|c| [ c.name, "#{c.id}"] }, {:include_blank => true, :selected => params[:search] ? "#{params[:search][:correspondents_id_equals]}" : nil}, :class => "select_refine_search" %></td>
  </tr>
  <% end %>
<tr>
  <td class="label"><label>Marque/Constructeur : </label></td>
  <td><%= f.select :brand_equals, Team.all.collect {|a| [ a.name, a.name ] }, {:include_blank => true, :selected => params[:search] ? params[:search][:brand_equals] : nil}, :class => "select_refine_search" %></td>
</tr>       