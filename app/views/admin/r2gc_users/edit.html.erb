<h1>Edition de l'utilisateur <%= @r2gc_user.name %></h1>
<% form_for([:admin, @r2gc_user]) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p class="buttons">
    <%= submit_tag "Enregistrer", :class => "button" %>
     ou <%= link_to "Annuler", :back %>
  </p>
<% end %>