<h1>Nouveau labo</h1>
<% semantic_form_for([:admin, @laboratory]) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p class="buttons">
    <%= submit_tag "Enregistrer", :class => "button" %>
  </p>
<% end %>