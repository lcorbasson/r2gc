<div class="outset">
  <table class="index">
    <thead>
      <tr>        
        <th>Sous type</th>
        <th>Type</th>
        <th>Modifier</th>
      </tr>
    </thead>
    <tbody>
      <% @tool_subtypes.each do |tool_subtype| %>
        <tr>          
          <td><%= link_to tool_subtype.name, edit_admin_tool_subtype_path(tool_subtype) %></td>
          <td><% subtype =   case tool_subtype.tool_type
                      when "TestEngine" : "Moyen d'essai"
                      when "ToolEngine" : "Machine outil"
                      when "MeasuringEngine" : "Moyen de mesure et d'analyse"
                      when "Software" : "Logiciel"

          end %><%= subtype %></td>

          <td><%= link_to image('remove', :alt => 'supprimer sous type')+"Supprimer", admin_tool_subtype_path(tool_subtype), :method => :delete, :confirm => "Etes-vous sûr de vouloir supprimer ce sous type ?" %></td>
          
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<div id="actions">
  <ul>
    <li><%= link_to "Nouveau sous type", new_admin_tool_subtype_path() %></li>
  </ul>
</div>
