<h1>Edition de la machine outil <%= @tool_engine.name %></h1>
<% form_for([:admin, @tool_engine]) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p class="buttons">
    <%= submit_tag "Enregistrer", :class => "button" %>
  </p>
<% end %>