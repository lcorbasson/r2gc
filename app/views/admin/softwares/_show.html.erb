<td><%= link_to tool.name, edit_admin_software_path(tool) %></td>
<td><%= tool.type %></td>
<td><%= tool.tool_subtype %></td>
<td><%= link_to image('remove', :alt => 'remove page'), admin_tool_path(tool), :method => :delete, :confirm => "Etes-vous sûr de vouloir supprimer cet équipement ?" %></td>