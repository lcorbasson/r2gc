<h1>Nouvel organisme</h1>
<% semantic_form_for([:admin, @organism]) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p class="buttons">
    <%= submit_tag "Enregistrer", :class => "button" %>
  </p>
<% end %>