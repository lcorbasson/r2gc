<h1>Edition du statutlogiciel <%= @status.name  %></h1>
<% form_for([:admin, @status]) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p class="buttons">
    <%= submit_tag "Enregistrer", :class => "button" %>
  </p>
<% end %>