<h1>Edition des conditions d'utilisation</h1>
<% semantic_form_for([:admin, @use_condition]) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p class="buttons">
    <%= submit_tag "Enregistrer", :class => "button" %>
  </p>
<% end %>